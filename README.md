# DM-Homework_02
## Dataset

This project uses DVC to track the dataset stored in the `data/` directory. After configuring the DVC remote, run `dvc pull` to restore the dataset if it is not already available locally.
